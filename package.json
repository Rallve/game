{
  "name": "game",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=build.js",
    "watch": "esbuild --watch main.js --bundle --outfile=build.js",
    "start": "esbuild main.js --bundle --minify --sourcemap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.15"
  }
}
